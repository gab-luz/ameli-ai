# one kv for every page
# gif appears in the center of the page
# gif is always spinning but not too fast
# when you touch on the gif, it'll play a beep
# and it will sping a little bit faster enough so that the user can notice
# when you touch the gif file, it will say something like "I'm here". "What do you need?"
# o include permite incluir outros arquivos kv

#:include kv/homepage.kv



ScreenManager:
    id: screen_manager # id que será usado no arquivo .py
    HomePage: # mesmo nome que usei em main.py para a classe
        name: "homepage"
        id: homepage
